<nav>
  <% if user_signed_in? %>
    <%= link_to 'Meus planos', my_plans_path %> |
    <%= link_to('Sair', destroy_user_session_path, method: :delete) %>
  <% else %>
    <%= link_to 'Entrar', new_user_session_path %>
  <% end %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
</nav>
<hr>
