<h3>Plano de Viagem</h3>
<%= link_to 'Criar novo plano', new_trip_plan_path  %>
<hr>
<% if @trip_plans.present? %>
  <% @trip_plans.each do |trip_plan|  %>
    <h5><%= link_to trip_plan.title, trip_plan_path(trip_plan.id) %></h5>
    <ul>
      <li>Data Início: <%= trip_plan.start_date.strftime('%d/%m/%Y') %></li>
      <li>Data Fim: <%= trip_plan.end_date.strftime('%d/%m/%Y') %></li>
    </ul>
    <hr>
  <% end %>
<% else %>
  <p>Não existem planos disponíveis</p>
<% end %>
<%= link_to 'Voltar', root_path %>
